package backend;

/**
 * A class with all the settings that are used in game.
 */
class GameSettings
{
    public static var downscroll:Bool = false;
    public static var ghostTapping:Bool = false;
    public static var strumBackground:Int = 85;
    public static var hitSound:Bool = true;
}